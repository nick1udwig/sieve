Guidance prompt family.

`system.md` is the qLLM guidance system prompt used by `sieve-llm`.

`instruction.md` is the app-side instruction appended by `sieve-app` when it renders the guidance payload.

Keep both files together because they define one guidance contract.
