Heartbeat prompt family.

`idle.md` is used when heartbeat fires without queued events.

`events.md` is used when heartbeat delivers queued main-session events.

Timestamp, reason, and event assembly stays in `sieve-app`.
