Shared prompt assets for Sieve crates.

Organize by prompt family, not by caller crate.

Keep rendering logic in the consuming crate.

Use each family README to explain which crate assembles context and which crate sends the prompt to a model.
